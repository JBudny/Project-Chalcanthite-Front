// @flow

import type { StyledComponent } from 'styled-components';
import styled from 'styled-components';

const TextWrapper: StyledComponent<any, any, HTMLParagraphElement> = styled.p`
  margin: 0.5rem 0;
  color: #999;
`;

export default TextWrapper;
