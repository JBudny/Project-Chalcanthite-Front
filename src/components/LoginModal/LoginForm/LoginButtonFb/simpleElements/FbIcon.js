// @flow

import type { StyledComponent } from 'styled-components';
import styled from 'styled-components';

const FbIcon: StyledComponent<any, any, HTMLSpanElement> = styled.span`
  display: inline-block;
  margin-right: 0.5rem;
  font-size: 1.5rem;
`;

export default FbIcon;
