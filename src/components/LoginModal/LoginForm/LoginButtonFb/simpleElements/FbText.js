// @flow

import type { StyledComponent } from 'styled-components';
import styled from 'styled-components';

const FbText: StyledComponent<any, any, HTMLSpanElement> = styled.span`
  display: inline-block;
  vertical-align: top;
  line-height: 1.5;
  font-weight: 500;
`;

export default FbText;
