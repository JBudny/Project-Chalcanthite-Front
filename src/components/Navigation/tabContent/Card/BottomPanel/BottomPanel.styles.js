// @flow

import type { StyledComponent } from 'styled-components';
import styled from 'styled-components';

const BottomPanelWrapper: StyledComponent<any, any, HTMLDivElement> = styled.div`
  text-align: center;
  font-size: 20px;
  margin: 0 0.4rem;
`;

export default BottomPanelWrapper;
