// @flow

import type { StyledComponent } from 'styled-components';
import styled from 'styled-components';

const TitlePanelWrapper: StyledComponent<any, any, HTMLDivElement> = styled.div`
  margin: 0 0.4rem;
`;

export default TitlePanelWrapper;
