import styled from 'styled-components';

// $FlowSuppressError: No need for type checking here at this moment
const EvenlyDistributed = styled.div`
  display: flex;
  justify-content: space-between;
`;

export default EvenlyDistributed;
