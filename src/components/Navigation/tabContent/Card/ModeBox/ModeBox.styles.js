// @flow

import type { StyledComponent } from 'styled-components';
import styled from 'styled-components';

const ModeBoxWrapper: StyledComponent<any, any, HTMLDivElement> = styled.div`
  margin: 5px 0;
`;

export default ModeBoxWrapper;
